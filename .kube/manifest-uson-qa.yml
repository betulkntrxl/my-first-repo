---
apiVersion: v1
kind: Secret
metadata:
  name: mt-openai-chat-app-uson-secrets-qa
  namespace: dwa-mobile-qa
type: Opaque
stringData:
  OKTA_CLIENT_ID: ${USON_OKTA_CLIENT_ID}
  OKTA_CLIENT_SECRET: ${USON_OKTA_CLIENT_SECRET}
  OKTA_ISSUER: ${USON_OKTA_ISSUER}
  MULESOFT_OPENAI_CLIENT_ID: ${MULESOFT_OPENAI_CLIENT_ID}
  MULESOFT_OPENAI_CLIENT_SECRET: ${MULESOFT_OPENAI_CLIENT_SECRET}
  EXPRESS_SESSION_SECRET: ${EXPRESS_SESSION_SECRET}
  REDIS_HOST: ${REDIS_HOST}
  REDIS_KEY: ${REDIS_KEY}
  ALLOW_TEST_USER: "${ALLOW_TEST_USER}"
  TEST_USER_API_KEY: "${TEST_USER_API_KEY}"
  APPLICATION_INSIGHTS_CONNECTION_STRING: ${USON_APPLICATION_INSIGHTS_CONNECTION_STRING}

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: mt-openai-chat-app-uson-config-qa
  namespace: dwa-mobile-qa
data:
  DEPLOY_ENVIRONMENT: cloud
  ORG_DEPLOYMENT: uson
  OKTA_APP_BASE_URL: https://uat-chatapp.usoncology.com
  PORT: "8080"
  LOG_LEVEL: info
  VERSION: ${IMAGE_TAG}
  SESSION_EXPIRY_IN_MILLISECONDS: "1800000"
  MULESOFT_OPENAI_CHAT_API_URL: "https://apimulewest-tst.nonprod.az.mckesson.com/inteng-az-openai-proxy-test/chat"
  MULESOFT_OPENAI_CHAT_API_URL_GPT4: "https://apimulewest-tst.nonprod.az.mckesson.com/inteng-az-openai-proxy-test/chat/v2"
  NODE_EXTRA_CA_CERTS: "certs/mule-chain.pem"

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mt-openai-chat-app-uson-qa
  namespace: dwa-mobile-qa
spec:
  selector:
    matchLabels:
      app: mt-openai-chat-app-uson-qa
  strategy:
    rollingUpdate:
      maxSurge: 50%
      maxUnavailable: 0%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: mt-openai-chat-app-uson-qa
    spec:
      containers:
        - name: mt-openai-chat-app-uson-qa
          image: "${DOCKER_REGISTRY}/mt-openai-chat-app:${IMAGE_TAG}"
          ports:
            - containerPort: 8080
          readinessProbe:
            failureThreshold: 6
            httpGet:
              path: /api/version
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 30
            successThreshold: 1
            timeoutSeconds: 15
          imagePullPolicy: Always
          resources:
            limits:
              cpu: 500m
              memory: 1Gi
            requests:
              cpu: 250m
              memory: 500Mi
          envFrom:
            - secretRef:
                name: mt-openai-chat-app-uson-secrets-qa
            - configMapRef:
                name: mt-openai-chat-app-uson-config-qa
      imagePullSecrets:
        - name: docker-registry-credentials

---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: mt-openai-chat-app-uson-qa-hpa
  namespace: dwa-mobile-qa
spec:
  scaleTargetRef:
    apiVersion: apps/v2beta2
    kind: Deployment
    name: mt-openai-chat-app-uson-qa
  minReplicas: 2
  maxReplicas: 6
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: 70

---
apiVersion: v1
kind: Service
metadata:
  name: mt-openai-chat-app-uson-qa-svc
  namespace: dwa-mobile-qa
  labels:
    app: mt-openai-chat-app-uson-qa
spec:
  ports:
    - protocol: TCP
      port: 8080
  selector:
    app: mt-openai-chat-app-uson-qa
  type: ClusterIP
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mt-openai-chat-app-uson-qa-ingress
  namespace: dwa-mobile-qa
  annotations:
    kubernetes.io/ingress.class: nginx-internal
    nginx.ingress.kubernetes.io/proxy-cookie-path: |
      / "/; HTTPOnly; Secure;"
spec:
  tls:
    - hosts:
        - uat-chatapp.usoncology.com
      secretName: ingress-uson-tls-qa
  rules:
    - host: uat-chatapp.usoncology.com
      http:
        paths:
          - backend:
              service:
                name: mt-openai-chat-app-uson-qa-svc
                port:
                  number: 8080
            pathType: ImplementationSpecific
